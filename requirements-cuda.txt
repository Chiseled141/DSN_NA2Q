# Install this INSTEAD of requirements.txt on a CUDA machine
# Installs PyTorch with CUDA 12.1 support (works on most cloud GPUs)
#
#   pip install -r requirements-cuda.txt

--index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0

# Then install the rest normally:
#   pip install -r requirements.txt --ignore-installed torch torchvision
numpy>=1.24.0
scipy>=1.10.0
gymnasium>=0.29.0
tqdm>=4.65.0
matplotlib>=3.7.0
tensorboard>=2.13.0
jinja2>=3.1.0
